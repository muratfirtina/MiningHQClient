export enum OperatorLicense {
    None = 0,
    Loader = 1,                    // Yükleyici
    Excavator = 2,                 // Ekskavatör
    LoaderAndExcavator = 3,        // Loader + Ekskavatör
    Drill = 4                      // Delici
}
